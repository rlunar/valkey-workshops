# Database Configuration
# Copy this file to .env and update with your database credentials

# Database type: mysql, mariadb, postgresql, or postgres
# This setting is used by backup/restore scripts to determine the correct tools to use
DB_TYPE=mysql

# Database connection details
DB_HOST=localhost
DB_PORT=3306
DB_NAME=flughafendb
DB_USER=username
DB_PASSWORD=password

# Optional: Full database URL (overrides individual settings if provided)
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/flughafendb
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/flughafendb

# Backup Configuration Notes:
# - For MySQL/MariaDB: Requires mysql and mysqldump clients
# - For PostgreSQL: Requires psql and pg_dump clients
# - Backups are stored in ./backups/ directory
# - Old backups (>7 days) are automatically cleaned up
# - Backups are automatically compressed with gzip if available
#
# Usage:
#   ./scripts/test-backup.sh    # Test configuration
#   ./scripts/backup.sh         # Create backup
#   ./scripts/restore.sh        # Restore from backup (interactive)
#   ./scripts/restore.sh file   # Restore from specific file